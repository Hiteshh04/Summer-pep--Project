# Copy this file to ".env" and fill in your real MySQL credentials.
# NEVER commit the real ".env" file to GitHub — it is already excluded
# in .gitignore.

DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=car_rental_analytics
